#include<stdio.h>
#include<string.h>
int main()
{ 
    char ch[]="harsh";
    int a=strlen(ch);
    printf("the length od string is %d\n",a);
    return 0;
}
// function is used to find length of string